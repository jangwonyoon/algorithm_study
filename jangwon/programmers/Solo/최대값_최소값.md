```js
function solution(s) {
    let result = '';
    let arr = [];
    let str = s.split(' ').map((el)=>+el);
    arr.push(Math.min(...str));
    arr.push(Math.max(...str));
    
    arr.map((el)=>{
        result += el + ' ';
    })
    
    return result.trim();
}
```